{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "start": { "cache": false },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "ui:build": { "cache": false },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
